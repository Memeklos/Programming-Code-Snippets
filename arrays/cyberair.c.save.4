#include <stdio.h>

int reservestat(int a);

int main() {

	int reserve;
	int cabin = 0;

	while (cabin < 10) {
		reservestat(reserve);
		printf("Which seat would you like to reserve? ");
			scanf("%d", &reserve);
		printf("Seat #%d reserved!\n", reserve);
		cabin++;
	}

	printf("'This is your pilot speaking. We are clear for takeoff!'\n\n");

	return 0;
}

int reservestat(int r) {

	int seats[9] = {0};

	if (seats[0] = 0) {
		seat[0] = 1;
	}
	if else (seats[1] = 0) {
		seat[1] = 1;
	}
	if else (seats[3] = 0) {
		seat[3] = 1;
	}
	if else (seat[4] = 0) {
		seat[4] = 1;
	}
	if else (seat[5] = 0) {
		seat[5] = 1;
	}
	if else (seat[6] = 0) {
		seat[6] = 1;
	}
	if else (seat[7] = 0) {
		seat[7] = 1;
	}
	if else (seat[8] = 0) {
		seat[8] = 1;
	}
	if else (seat[9] = 0) {
		seat[9] = 1;
	}
	if else (seat[10] = 0) {
		seat[10] = 1;
	}
	else {
		printf("That seat is already reserved!\n");
	}



	printf("\nCurrent reservation status:\n");
	
printf("Seat #1: %d\n", a);
	printf("Seat #2: %d\n", b);
	printf("Seat #3: %d\n", c);
	printf("Seat #4: %d\n", d);
	printf("Seat #5: %d\n", e);
	printf("Seat #6: %d\n", f);
	printf("Seat #7: %d\n", g);
	printf("Seat #8: %d\n", h);
	printf("Seat #9: %d\n", i);
	printf("Seat #10: %d\n", j);

}